# summerResearch26

In nMessageModelManual.py you set
sigma, k, but also x_a, x_b, x_c... and c_ab, c_bc... manually inside the code

basicModel.py was downgraded from nMessageModelManual.py to only work with 2 messages but optimize
for x_a and x_b for the given sigma, k. c is set to 0.5